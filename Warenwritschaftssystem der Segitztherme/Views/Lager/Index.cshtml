@model IEnumerable<Warenwritschaftssystem_der_Segitztherme.Models.Lager>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Beschreibung)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Beschreibung)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LagerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LagerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LagerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
